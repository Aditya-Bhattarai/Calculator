def subtraction():
    first_input = float(input('Enter your first number(the number to subtract): '))
    second_input = float(input('Enter your second number(the number it should be subtracted by): '))
    return first_input-second_input