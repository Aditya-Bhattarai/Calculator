def power():
    first_input = float(input('Enter the number to do power with(base number): '))
    second_input = float(input('Enter the number to power (coefficient number): '))
    print(first_input**second_input)