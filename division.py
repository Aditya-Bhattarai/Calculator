def divide():
    first_input = float(input('Enter the number to divide from(dividend): '))
    second_input = float(input('Enter the number to divide with(divisor): '))
    print(first_input/second_input)